config:
    send_anonymous_usage_stats: False
    use_colors: True

dbt_data_quality_demo:
  target: alert
  outputs:
    alert:
      type: snowflake
      account: infinitelambda.eu-west-1
      warehouse: DEMO_WH
      database: MINHNGO_DEV_DATABASE
      role: MINHNGO_DEV_ROLE
      schema: DIQU_DEV_AUDIT
      user: minh@infinitelambda.com
      password: "{{ env_var('DBT_ENV_SECRET_SNOWFLAKE_PASSWORD') }}"
      threads: 4